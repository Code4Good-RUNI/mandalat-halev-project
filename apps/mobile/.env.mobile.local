# Used when EXPO_APP_TARGET=local (see root package.json script mobile:start:local).
# iOS simulator and Expo web: 127.0.0.1 reaches the host. Android emulator: use http://10.0.2.2:3000 (or your LAN IP for a physical device).
EXPO_PUBLIC_API_BASE_URL=http://localhost:3000/api
